node_modules/
dist/
out/
release/
*.log
.DS_Store
.env
.env.*
*.local
coverage/
.vite/

# Scratch render harnesses — bundled output, never part of the app.
.harness.*

# Incremental typecheck caches — regenerated by tsc, never worth committing.
*.tsbuildinfo

# The Node server's SQLite database. Created the moment src/server boots — a
# real database with real rows, in a PUBLIC repo. Ignored rather than remembered.
data/
